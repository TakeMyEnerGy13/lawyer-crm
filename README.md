# Кабинет юриста — мини-CRM (тестовое задание)

Живая ссылка: **https://takemyenergy13.github.io/lawyer-crm/**

Для быстрой проверки — кнопка **«Попробовать без регистрации»** (гостевой профиль в один клик) или готовый профиль с наполненной базой: `demo-a@lawyer-crm.test` / `demoA123456`. База у каждого профиля своя.

Одностраничный дашборд для юриста: таблица клиентов, добавление клиента (имя, телефон, суть дела, статус), смена статуса прямо в строке, живые счётчики по статусам с фильтрацией. Данные общие для всех посетителей и обновляются в реальном времени (открой в двух вкладках — изменения синхронизируются без перезагрузки).

Сверх ТЗ (маленькие фичи, которые делают инструмент рабочим):
- авторизация (email/пароль) с изоляцией данных: каждый юрист видит только своих клиентов, доступ проверяется серверными Firestore rules;
- поиск по имени, телефону (в любом формате — матчится по цифрам) и сути дела;
- удаление клиента с подтверждением;
- поле «суть дела» у клиента;
- экспорт в CSV с выбором: текущая выборка / вся база / только добавленные после последнего экспорта, плюс сортировка (с BOM и `;` — открывается в русском Excel без кракозябр).

## Стек и почему

- **React + Vite + TypeScript** — быстрый старт, типобезопасность, знакомый стек.
- **Firebase Firestore** (вариант «React + Firebase» из ТЗ) — реальная база с realtime-подпиской `onSnapshot`, работает напрямую из статики без своего бэкенда.
- **GitHub Pages + GitHub Actions** — деплой на каждый push в `main`, тесты гоняются в CI перед сборкой.
- **Telegram-уведомление** — при добавлении клиента фронт шлёт fire-and-forget запрос напрямую в Telegram Bot API; сбой уведомления не ломает добавление клиента. Уведомления падают в публичный канал **[t.me/TestoviyALerts](https://t.me/TestoviyALerts)** — откройте его, добавьте клиента на сайте и увидите уведомление в реальном времени. Общий публичный канал — демо-упрощение, чтобы бонус можно было проверить самостоятельно; в проде уведомления шли бы каждому юристу в личку по `ownerId`, с сервера.

## Архитектура

```
Браузер (React SPA) ──realtime──▶ Firestore (коллекция clients, доступ по ownerId)
        │
        └──POST при добавлении──▶ Telegram Bot API (уведомление юристу)
```

Вся производная логика (счётчики, фильтр) — чистые функции (`src/lib/clients-logic.ts`), покрыты unit-тестами (vitest).

## Безопасность и компромиссы

- **Firestore rules по владельцу**: читать/менять/удалять запись может только её автор (`ownerId == auth.uid`), правила исполняются на серверах Google. Без логина коллекция недоступна (403).
- **Firebase web config в коде** — это публичные идентификаторы, так задумано Firebase; секретов в клиенте нет.
- **Токен Telegram-бота виден в бандле** — осознанный компромисс для тестового: бот одноразовый, заведён только под уведомления этого прототипа, worst case — кто-то отправит сообщения от его имени. В проде токен жил бы на сервере (webhook/функция). После проверки токен отзывается в BotFather.
- Компромиссы: нет подтверждения почты и восстановления пароля, нет редактирования клиента и пагинации — вне скоупа тестового.

Для проверки: зарегистрируйте любой профиль прямо на сайте (почта не подтверждается) — база у каждого профиля своя.

## Запуск локально

```bash
npm install
npm run dev      # http://localhost:5173/lawyer-crm/
npm test         # unit-тесты логики
```

## Краткий лог (что сам / что AI)

- **Сам:** декомпозиция и приёмка решений (стек, схема данных, компромиссы безопасности, дизайн-направление, что войдёт сверх ТЗ), создание Firebase-проекта и Telegram-бота, включение Auth-провайдера и публикация Firestore rules в консоли, ревью кода и результата на каждом шаге.
- **AI (Claude Code):** спека и план по моим ответам, генерация кода по плану (TDD для чистой логики), автосмоук в headless-браузере после каждой фичи (регистрация, изоляция профилей, статусы, фильтры, экспорт, мобильная вёрстка), диагностика бага с невключённым Firestore API по сетевым логам.
- **Время:** 10 июля, 16:00–18:45; чистого времени меньше — работал с перерывами, параллельно с основной работой.
- **Чего не успел:** прогнать прототип на реальном юристе. Первые вопросы, которые задал бы: хватает ли трёх статусов или нужны свои этапы (суд первой инстанции, апелляция…); что важнее в таблице — телефон или ближайшее событие по делу; нужен ли экспорт для отчётности или это моя гипотеза.
